[workspace]
resolver = "2"

members = [
    "crates/wie_backend",
    "crates/wie_base",
    "crates/wie_core_arm",
    "crates/wie_vendor_ktf",
    "crates/wie_wipi_c",
    "crates/wie_wipi_java",
]

[workspace.dependencies]
anyhow = { version = "^1.0", default-features = false }
async-trait = { version = "^0.1", default-features = false }
bitflags = { version = "^2.3", default-features = false }
bytemuck = { version = "^1.13", default-features = false, features = ["derive"] }
futures = { version = "^0.3", features = ["alloc"], default-features = false }
tracing = { version = "^0.1", default-features = false, features = ["attributes"] }

[package]
name = "wie"
version = "0.0.1"
edition = "2021"
license = "MIT"

[dependencies]
anyhow = { version = "^1.0", features = ["backtrace"] }
tracing = { workspace = true }
tracing-subscriber = { version = "^0.3" }
zip = { version = "^0.6" }

wie_backend = { path = "crates/wie_backend" }
wie_vendor_ktf = { path = "crates/wie_vendor_ktf" }
